# Notebooks

Use this folder for experiment notes, dataset exploration, and result analysis. Core model code is intentionally kept outside notebooks so it can be reused by training scripts and the dashboard.
